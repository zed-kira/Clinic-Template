// const menuClick = () => {
//     const menuToggle = document.querySelector('.menu');
//     const imgClick = document.querySelector('.image');

//     imgClick.addEventListener('click', () => {
//         menuToggle.classList.toggle('active');
//     });
// };

// menuClick();

var swiper = new Swiper(".mySwiper", {
  effect: "cards",
  grabCursor: true,
});